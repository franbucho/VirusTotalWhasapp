{
  "name": "whatsapp-virustotal-bot",
  "version": "1.0.0",
  "main": "bot.js",
  "scripts": {
    "start": "pm2 start bot.js --no-daemon",
    "build": "npm install && npm install -g pm2",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "whatsapp-web.js": "^1.23.0",
    "axios": "^1.6.7",
    "express": "^4.18.2",
    "qrcode-terminal": "^0.12.0",
    "dotenv": "^16.3.1",
    "form-data": "^4.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}